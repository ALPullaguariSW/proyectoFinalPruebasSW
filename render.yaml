services:
  - type: web
    name: plataforma-reservas-backend
    env: node
    plan: free
    buildCommand: cd backend && npm install
    startCommand: |
      cd backend
      # Inicializar base de datos si es necesario
      npm run init-db || echo "Base de datos ya inicializada"
      npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        value: postgresql://reservas_db_knfd_user:fPLvTe9gRVqQuhUSgcQmv7ehmNfDMqRk@dpg-d2j4m5gdl3ps738nulb0-a.oregon-postgres.render.com/reservas_db_knfd
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 10000
    healthCheckPath: /api/health
    autoDeploy: false
